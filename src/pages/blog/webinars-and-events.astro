const currentPath = Astro.url.pathname;
const lang = currentPath.startsWith('/fi') ? 'fi' : 'en';
const targetPath = lang === 'fi' ? '/fi/resources' : '/resources';

return Astro.redirect(targetPath, 301);